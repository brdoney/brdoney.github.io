markdown: kramdown
highlighter: rouge
kramdown:
  input: GFM

exclude:
  - scripts

collections:
  documents:
    output: true
    sort_by: order

defaults:
  - scope:
      path: "_documents"
    values:
      layout: "document"
